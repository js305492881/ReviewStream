{
  "name": "git-push-review",
  "displayName": "git push review",
  "description": "在 VS Code 的源代码管理（SCM）界面为每个 Git 仓库添加一个 “Push for Review” 按钮，点击后自动 push 当前分支到远端 refs/for/分支名，适用于 Gerrit 代码评审场景，支持自动识别评审 URL、弹窗与 macOS 系统通知提示。",
  "version": "1.1.0",
  "engines": {
    "vscode": "^1.116.0"
  },
  "repository": {
    "type": "git",
    "url": "http://gitlab.alibaba-inc.com/wh_stc_mw/vscode_extension_gitpushreview.git"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.gitPushForReview",
        "title": "Push for Review",
        "icon": {
          "light": "images/Push.svg",
          "dark": "images/Push-dark.svg"
        }
      }
    ],
    "menus": {
      "scm/title": [
        {
          "when": "scmProvider == git",
          "command": "extension.gitPushForReview",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "~18.19.130",
    "@types/node-notifier": "^8.0.5",
    "@types/vscode": "^1.116.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^8.57.1",
    "ts-loader": "^9.5.7",
    "typescript": "^5.9.3",
    "vsce": "^2.15.0",
    "webpack": "^5.106.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "node-notifier": "^10.0.1"
  }
}
